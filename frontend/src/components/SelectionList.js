

export default function SelectionList({title}){
	return (
		<div className={"h-full p-4 w-1/6 overflow-y-auto rounded-lg bg-gray-100"}>
			<h1 className={"font-semibold"}>{title}</h1>
		</div>
	)
}
