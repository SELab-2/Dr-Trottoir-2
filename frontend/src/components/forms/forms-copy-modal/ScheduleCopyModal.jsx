import BasicCopyModal from "@/components/forms/forms-copy-modal/BasicCopyModal";

export default function ScheduleCopyModal({ open, onCloseModal }) {
  return (
    <BasicCopyModal onCloseModal={onCloseModal} open={open}>
      <p>TODO</p>
    </BasicCopyModal>
  );
}
