name: "Tests CI"
on:
  - push

jobs:
  test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - name: Build the image
        run: docker-compose up -d --build
      - name: Test Django unittest
        run: docker exec DrTrottoir-be-d python manage.py test
      - name: Test NEXT.js Jest
        run: docker exec DrTrottoir-fe-d npm run jest:workflow